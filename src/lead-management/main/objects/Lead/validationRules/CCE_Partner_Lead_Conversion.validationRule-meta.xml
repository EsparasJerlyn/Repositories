<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CCE_Partner_Lead_Conversion</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    OR(
        ISPICKVAL(Status, &apos;New&apos;),
        ISPICKVAL(Status, &apos;Qualified&apos;)
    ),
    OR(
        ISBLANK(Address),
        ISBLANK(FirstName),
        AND(
            ISBLANK(Phone),
            ISBLANK(MobilePhone)
        )
    )
)</errorConditionFormula>
    <errorMessage>One of the required fields for a qualified CCE Partner Lead is missing: Phone, Name, Address. Please populate all required fields.</errorMessage>
</ValidationRule>
