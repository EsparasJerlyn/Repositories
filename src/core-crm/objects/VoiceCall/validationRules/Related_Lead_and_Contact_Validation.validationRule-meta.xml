<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Related_Lead_and_Contact_Validation</fullName>
    <active>true</active>
    <description>Lead and Contact cannot be linked to the Voice Call at the same time.</description>
    <errorConditionFormula>AND(
	NOT(ISBLANK(Related_Contact__c)), 
	NOT(ISBLANK(Related_Lead__c))
)</errorConditionFormula>
    <errorMessage>Lead and Contact cannot be linked to the Voice Call at the same time.</errorMessage>
</ValidationRule>
